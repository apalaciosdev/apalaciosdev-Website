import {Colors} from '../colors'


const card = {
  generalDivCard: {
    display: 'flex',
    justifyContent: 'center',

  },
  
  divCard: {
    display: 'grid',
    justifyItems: 'center',
    border: '1px solid black',
  },

  divProjects: {
    
  }
}

export default card;