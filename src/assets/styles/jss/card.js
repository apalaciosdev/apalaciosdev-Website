import {Colors} from '../colors'


const card = {
  generalDivCard: {
    display: 'flex',
    justifyContent: 'center',

  },
  
  divCard: {
    display: 'flex',
    justifyContent: 'center',
    maxWidth: 790,
    width: '70vw',
    maxHeight: 700,
    height: '70vh',
    boxShadow: 'rgba(0, 0, 0, 0.4) 3px 5px 50px',
    marginTop: 80,
  },

  imgCard: {
    height: '100%',
  }
}

export default card;