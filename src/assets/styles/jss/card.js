import {Colors} from '../colors'


const card = {
  generalDivCard: {
    display: 'flex',
    justifyContent: 'center',

  },
  
  divCard: {
    display: 'grid',
    justifyItems: 'center',
    border: '1px solid black',
    maxWidth: 790,
    width: '70vw',
  },

  imgCard: {
    height: '80px',
  }
}

export default card;